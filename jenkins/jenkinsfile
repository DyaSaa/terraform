pipeline {
    agent any
    stages {
    stage('Terraform Init') {
      steps {
        sh 'cd C:\Users\Ramesh G\Desktop\cert'
        git branch: 'main', url: 'https://github.com/DyaSaa/terraform.git'
        
        sh 'terraform init'
        
      }
    }
  }
}

